services:
  web:
    build: .
    image: availability-web:latest
    pull_policy: build
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - availability_data:/data
    restart: unless-stopped

volumes:
  availability_data:
